(ns ktra-indexer.parser
  (:require [clojure.string :as s]))

(defn parse-tracklist
  "Parses a KTRA episode tracklist, preferrably from Soundcloud. Returns
  a map of tracks with their associated episode features."
  []
  true)
