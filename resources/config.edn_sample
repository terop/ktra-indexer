{:database {
            :host "localhost"
            :db "ktra"
            :port "5432"
            :user "foobar"
            :password "123"
            }
 ;; Time zone to use in database operations
 :time-zone "Europe/Helsinki"
 ;; Yubico OTP parameters
 :yubico-client-id "12345"
 :yubico-secret-key "/xyz="
 ;; Toggle development or production mode
 :in-production true
 ;; Use through proxy (usually needed when a reverse proxy is used)
 :use-proxy true}
